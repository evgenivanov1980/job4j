package lambda;

import java.util.Comparator;

public class Fi  {
    public static void main(String[] args) {
        Comparator<String> str1 = (left, right) -> left.compareTo(right);
        
    }
}
